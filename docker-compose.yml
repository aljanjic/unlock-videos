services:
  web:
    build: .
    command: gunicorn  --timeout 300 unlock_videos.wsgi:application --bind 0.0.0.0:8002
    volumes:
      - .:/app
    ports:
      - "8002:8002"
    env_file:
      - ./.env
    working_dir: /app